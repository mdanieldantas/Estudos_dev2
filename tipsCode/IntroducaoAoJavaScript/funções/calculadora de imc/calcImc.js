/*function calImc(peso, altura) {
  let imc = peso / (altura * altura);
  let imcArredondado = Math.round(imc);

  return console.log(`Seu Imc é: ${imcArredondado} .`)
}

calImc(99, 1.80)

*/

function minhaFunction() {
  let a = 12
  let b = 7
  return a - b
}
console.log(minhaFunction())